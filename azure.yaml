# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: wrok
metadata:
    template: azd-init@1.17.2
services:
    wrok-apphost:
        project: Wrok.AppHost
        host: containerapp
        language: dotnet
resources:
    wrok-apphost:
        type: host.containerapp
        port: 8080
